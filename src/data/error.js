import Enum from 'enum';

export const allError = new Enum([
  'notEnoughMoney',

  'car_cantUpgrade',

  'player_notFree',
  'player_noActive',
  'player_queueFull',
]);

export const placeHolder = [];
