export default class Package {
  constructor(from, to, good, value, distance) {
    this.from = from;
    this.to = to;
    this.good = good;
    this.value = value;
    this.distance = distance;
  }
}
